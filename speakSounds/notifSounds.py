#!/usr/bin/python
#!/usr/bin/env python
  
import winsound

def sound_error():
    winsound.Beep(450,100) # (frecuencia[Hz], duration[ms])
    winsound.Beep(300,100) # (frecuencia[Hz], duration[ms])
    winsound.Beep(250,100) # (frecuencia[Hz], duration[ms])
    winsound.Beep(450,100) # (frecuencia[Hz], duration[ms])
    winsound.Beep(300,100) # (frecuencia[Hz], duration[ms])
    winsound.Beep(250,100) # (frecuencia[Hz], duration[ms])